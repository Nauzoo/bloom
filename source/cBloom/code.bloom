
#set: (numeric) short =>size(int)/2;
#set: (numeric >= 0) u_int => size int;

struct BIKE{
    string color;
    short price;

    fn (self:BIKE, string color, short price) bike => null {
        self.color = color;
        self.price = price;
    }
}

num:int = 13;

myBike:BIKE = bike("red", 400); ?? sets "self" parameter to myBike memory addr


